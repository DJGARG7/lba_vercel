import { Stack, Title } from "@mantine/core";
import { Header } from "../components/Header";

function About() {
    return (
        <Stack>
            <Header></Header>
            <Title>About</Title>
        </Stack>
    );
}

export default About;
